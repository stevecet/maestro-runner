// Shared Maestro configuration.
const config = {
  shortTimeout: 10000,
  mediumTimeout: 20000,
  longTimeout: 30000,
  extendedTimeout: 60000,
  veryLongTimeout: 200000,
};

output.config = config;
